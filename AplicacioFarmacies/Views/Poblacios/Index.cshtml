@model IEnumerable<AplicacioFarmacies.Models.Poblacio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Provincia.NomProvincia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomPoblacio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Provincia.NomProvincia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomPoblacio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPoblacio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPoblacio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPoblacio })
        </td>
    </tr>
}

</table>
